describe('Win easy opponent', function(){
    it('happy path', function() {
        cy.visit('/')

        // see start menu
        // don't see board

        // click start button

        // no longer see start menu
        // see game board
  })
})